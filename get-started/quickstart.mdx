---
title: "Quickstart"
description: "Get started with Undrstnd Developers and integrate powerful AI capabilities into your projects. This guide will walk you through getting an API key, making your first API request, and checking model statuses."
---

## Setup your development

The first step to unlocking world-class AI-powered applications is setting up your development environment with Undrstnd Developers.

### Get an API Key

To begin using the Undrstnd Developers API, you’ll need an API key.

<AccordionGroup>
  <Accordion icon="lock" title="Create an Account">
    Sign up for an account to access our platform and start building your AI
    applications. You can create an account on our
    [website](https://undrstnd-labs.com/register).
  </Accordion>
  <Accordion icon="key" title="Generate an API Key">
    Generate an API key to authenticate your requests and access our APIs. You
    can find your API key in the
    [dashboard](https://undrstnd-labs.com/dashboard/api-keys).
  </Accordion>
</AccordionGroup>

### Making Your First API Request

<AccordionGroup>

<Accordion icon="message-bot" title="HTTPie Example">
  ```bash
  http POST https://dev.undrstnd-labs.com/api \
  x-api-key:YOUR_API_KEY_UNDRSTND \
  stream=false \
  modelId="MODEL_ID" \
  messages:='[{"name": "system", "content": "Hello, how can I assist?", "role": "system"}, {"name": "user", "content": "What is the weather in San Francisco?", "role": "user"}]'
  ```
</Accordion>
<Accordion icon="rocket" title="cURL Example">
    ```bash
    curl -X POST https://dev.undrstnd-labs.com/api \
      -H "x-api-key: YOUR_API_KEY_UNDRSTND" \
      -H "Content-Type: application/json" \
      -d '{
        "stream": false,
        "modelId": "MODEL_ID",
        "messages": [
          {
            "name": "system",
            "content": "Hello, how can I help you?",
            "role": "system"
          },
          {
            "name": "user",
            "content": "What is the weather like in San Francisco?",
            "role": "user"
          }
        ]
      }'
  ```
</Accordion>
<Accordion title="Postman Example" icon="postman">
  1. Open Postman and create a new POST request.
  2. Enter the API endpoint `https://dev.undrstnd-labs.com/api`.
  3. In the Headers tab, add:
      - `x-api-key` with your API key.
      - `Content-Type` with `application/json`.
  4. In the Body tab, choose raw and select JSON format. Add this body:
  ```json
    {
    "stream": false,
    "modelId": "MODEL_ID",
    "messages": [
      {
        "name": "system",
        "content": "Hello, how can I help you?",
        "role": "system"
      },
      {
        "name": "user",
        "content": "What is the weather like in San Francisco?",
        "role": "user"
      }
    ]
  }
  ```
  5. Click Send to make the request.
</Accordion>

</AccordionGroup>

## Check Model Status

To view available models or check the status of an individual model, use the following endpoints:

<CardGroup>

<Card title="List All Models" icon="paintbrush" href="https://dev.undrstnd-labs.com/api/models/info">
  Get a list of all available models and their details.
</Card>

<Card
  title="Get Specific Model Info"
  icon="square-code"
  href="GET https://dev.undrstnd-labs.com/api/models/{MODEL_ID}/info"
>
  Retrieve detailed information about a specific model.
  Replace {MODEL_ID} with the actual model ID to get detailed information about a specific model.
</Card>

</CardGroup>
